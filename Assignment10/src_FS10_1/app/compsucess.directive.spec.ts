import { CompsucessDirective } from './compsucess.directive';

describe('CompsucessDirective', () => {
  it('should create an instance', () => {
    const directive = new CompsucessDirective();
    expect(directive).toBeTruthy();
  });
});
